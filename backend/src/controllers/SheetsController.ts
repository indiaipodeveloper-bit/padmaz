import { appendRows } from "../sheets/sheets.js";

export const createOrderInGoogleSheet = async (
  name: string,
  email: string,
  phone: string,
  address: string,
  city: string,
  zipCode: string,
  status: string = "Processingss",
  TotalPrice: string
) => {
  try {
    
  } catch (error) {
    console.log(error);
  }
};
